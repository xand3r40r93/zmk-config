#define ZMK_MOD_MORPH(name, behavior1, behavior2, ...) \
  name: name { \
    label = ZMK_MACRO_STRINGIFY(MM_ ## name); \
    compatible = "zmk,behavior-mod-morph"; \
    #binding-cells = <0>; \
    bindings = <behavior1>, <behavior2>; \
    __VA_ARGS__ \
  };
